{
  "name": "paperjammed-site",
  "version": "1.0.0",
  "private": true,
  "description": "PaperJammed printer prank generator",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
